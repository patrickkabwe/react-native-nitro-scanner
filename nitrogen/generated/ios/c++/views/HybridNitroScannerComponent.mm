///
/// HybridNitroScannerComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#import "HybridNitroScannerComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroScannerSpecSwift.hpp"
#import "NitroScanner-Swift-Cxx-Umbrella.hpp"

using namespace facebook;
using namespace margelo::nitro::nitroscanner;
using namespace margelo::nitro::nitroscanner::views;

/**
 * Represents the React Native View holder for the Nitro "NitroScanner" HybridView.
 */
@interface HybridNitroScannerComponent: RCTViewComponentView
@end

@implementation HybridNitroScannerComponent {
  std::shared_ptr<HybridNitroScannerSpecSwift> _hybridView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroScannerComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroScannerComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    std::shared_ptr<HybridNitroScannerSpec> hybridView = NitroScanner::NitroScannerAutolinking::createNitroScanner();
    _hybridView = std::dynamic_pointer_cast<HybridNitroScannerSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroScanner::HybridNitroScannerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) updateProps:(const react::Props::Shared&)props
            oldProps:(const react::Props::Shared&)oldProps {
  // 1. Downcast props
  const auto& newViewPropsConst = *std::static_pointer_cast<HybridNitroScannerProps const>(props);
  auto& newViewProps = const_cast<HybridNitroScannerProps&>(newViewPropsConst);
  NitroScanner::HybridNitroScannerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update each prop individually
  swiftPart.beforeUpdate();

  // enabled: boolean
  if (newViewProps.enabled.isDirty) {
    swiftPart.setEnabled(newViewProps.enabled.value);
    newViewProps.enabled.isDirty = false;
  }
  // onScan: function
  if (newViewProps.onScan.isDirty) {
    swiftPart.setOnScan(newViewProps.onScan.value);
    newViewProps.onScan.isDirty = false;
  }

  swiftPart.afterUpdate();

  // 3. Update hybridRef if it changed
  if (newViewProps.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newViewProps.hybridRef.value;
    if (maybeFunc.has_value()) {
      maybeFunc.value()(_hybridView);
    }
    newViewProps.hybridRef.isDirty = false;
  }

  // 4. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

@end
