///
/// HybridNitroScannerComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridNitroScannerComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitroscanner::views {

  extern const char HybridNitroScannerComponentName[] = "NitroScanner";

  HybridNitroScannerProps::HybridNitroScannerProps(const react::PropsParserContext& context,
                                                   const HybridNitroScannerProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    enabled([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("enabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.enabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.enabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroScanner.enabled: ") + exc.what());
      }
    }()),
    vibrateOnScan([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("vibrateOnScan", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.vibrateOnScan;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.vibrateOnScan);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroScanner.vibrateOnScan: ") + exc.what());
      }
    }()),
    onScan([&]() -> CachedProp<std::function<void(const NitroScannerResult& /* result */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onScan", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onScan;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const NitroScannerResult& /* result */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onScan);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroScanner.onScan: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::nitroscanner::HybridNitroScannerSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::nitroscanner::HybridNitroScannerSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroScanner.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridNitroScannerProps::HybridNitroScannerProps(const HybridNitroScannerProps& other):
    react::ViewProps(),
    enabled(other.enabled),
    vibrateOnScan(other.vibrateOnScan),
    onScan(other.onScan),
    hybridRef(other.hybridRef) { }

  bool HybridNitroScannerProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("enabled"): return true;
      case hashString("vibrateOnScan"): return true;
      case hashString("onScan"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroScannerComponentDescriptor::HybridNitroScannerComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  react::Props::Shared HybridNitroScannerComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                         const react::Props::Shared& props,
                                                                         react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroScannerShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroScannerComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridNitroScannerShadowNode&>(shadowNode);
    const HybridNitroScannerProps& props = concreteShadowNode.getConcreteProps();
    HybridNitroScannerState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitroscanner::views
